"""
DEPRECATED — replaced by app/retrieval/vector_store.py

This file is kept temporarily for reference only.
Do NOT import from it — all imports should use:

    from app.retrieval.vector_store import QdrantStorage, get_vector_store
"""
raise ImportError(
    "vector_db.py is deprecated. "
    "Import from app.retrieval.vector_store instead."
)